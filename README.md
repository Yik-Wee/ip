# Grug project

This is a greenfield Java project. It's named after a caveman named _Grug_, and forked from the SE-EDU _Duke_ template project.

# Requirements
1. JDK 25

# Running
Run the following command:
```
gradlew run
```
A GUI will pop up and you can start entering commands.

# Building JAR
Run the following command:
```
gradlew shadowJar
```
and a JAR will output to `build/libs/grug.jar`.
